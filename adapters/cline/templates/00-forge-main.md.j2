<!-- AUTO-GENERATED FROM erpnext-ai-forge {{ forge.version }} — DO NOT EDIT (re-run `forge sync --tool cline`) -->

# Forge — Architect & Specialists (Cline)

Operating in the **Novizna v16 ERPNext/Frappe bench**. The architect orchestrates; switch persona when the task type matches. Skill bodies are TOC-only — ask the developer to expand specific sections when needed.

---

## Bench Constraints

- **Never edit** `apps/{frappe,erpnext,crm,hrms,lending,lms,education,helpdesk,gameplan,drive,press}/` — upstream.
- Custom work in `apps/{novizna_crm,novizna_core,novizna_pos,invoice_ninja_integration,noviznaerp_payroll,cargo_management,changemakers,erpnext_location}/`.
- Never read `site_config.json` or `.env` **values** into context.
- `bench restart` requires typing the **site name** (`{{ bench.primary_site }}`).
- Type annotations + PEP 257 docstrings on every Python function.
- No `frappe.db.commit()` in `doc_events`. No f-string interpolation in `frappe.db.sql`.

---

## Architect Workflow

1. Pre-flight stale-reference check against `discovery/data/*.json`.
2. Draft TASK BRIEF: goal, constraints, acceptance, specialists, tools.
3. Switch persona to relevant specialist.
4. Run peer review (Backend → Security + QA; Frontend → QA; Integrations → Security + QA). 2-loop max.
5. Acceptance: 0 CRITICAL, 0 HIGH, score ≥ 95.
6. Closing documentation: per-app `CLAUDE.md`, `CHANGELOG.md`, ADRs.

---

## Specialist Personas

| Persona | When to switch |
|---------|----------------|
{% for agent in agents if agent.id != 'architect' -%}
| `{{ agent.id }}` | {{ agent.trigger | truncate(140) }} |
{% endfor %}

---

## Skills (TOC — expand on demand)

### Foundational
{% for skill in skills if skill.foundational -%}
- `{{ skill.id }}` ({{ skill.domain }})
{% endfor %}

### On-Demand
{% for skill in skills if not skill.foundational -%}
- `{{ skill.id }}` ({{ skill.domain }})
{% endfor %}

---

## Tools

| Tool | Purpose | Confirm |
|------|---------|:-------:|
{% for t in tools -%}
| `{{ t.id }}` | {{ t.purpose | truncate(100) }} | {{ '✓' if t.requires_confirmation else '' }} |
{% endfor %}

---

## Command Recipes

Cline has no slash commands; canonical commands are paste-able recipes:

{% for c in commands -%}
- **`/{{ c.id }}`** — {{ c.description | truncate(150) }}
{% endfor %}

---

<sub>Synced from `canonical/` in erpnext-ai-forge v{{ forge.version }} ({{ forge.source_commit | truncate(7, true, "") }}).</sub>
