{#-
  Header + legend for one ledger file.

  Rendered with artifact_kind="scaffold": written ONLY if the file is absent,
  never overwritten. Every other artifact is forge's to replace on each sync;
  this one accumulates rows written by agents, and the atomic swap would wipe
  them all on the next run.

  `phase` is bound by the renderer, once per file.
-#}
<!-- Header generated by erpnext-ai-forge {{ forge.version }}. Rows below are maintained by agents.
     forge writes this file only when it does not exist, and never touches it again. -->
# Build Ledger — {{ phase.title }}

{{ phase.blurb }}

**`build_state` values in this file:** {% for s in phase.states %}`{{ s }}`{% if not loop.last %}, {% endif %}{% endfor %}

A key appears in **exactly one** ledger file. Changing `build_state` means
*moving* the row to the file that owns the new state — delete here, append there.
Never copy. `forge validate` fails on a key found in two files.

`build_state` is agent-owned and lives here. The vault ticket's `status:` is
human-owned and lives there. They are not derived from each other, and neither
overrides the other — see `AGENTS-OPERATING-MANUAL.md`.

| KEY | build_state | agent | started | finished | last commit | notes |
|-----|-------------|-------|---------|----------|-------------|-------|
