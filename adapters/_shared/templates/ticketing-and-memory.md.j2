{#-
  Shared partial: renders the ticketing + brain contract to <target>/AGENTS-TICKETING.md.

  The TEXT lives in canonical/policies/ticketing-contract.md, not here. It used
  to live in this file, which meant a set of normative rules — vault layout,
  frontmatter schema, the binding force of `depends_on`, who owns `status:` —
  sat in the adapter layer with no `version:`, no `owners:`, no
  `last_reviewed:`, outside `forge score --path canonical/`, outside
  `forge validate`, and impossible to `forge deprecate`. That is a second source
  of truth inside the repo built to prevent them.

  Included by every adapter's root/aggregate template, so all seven tools ship
  identical text. Resolved at render time by the `_shared` templates dir on the
  Jinja ChoiceLoader (see forge/src/forge/render.py::_build_jinja_env).
-#}
<!-- AUTO-GENERATED FROM erpnext-ai-forge {{ forge.version }} — DO NOT EDIT (regenerated by `forge sync`) -->
{{ policies | selectattr("id", "eq", "ticketing-contract") | map(attribute="body") | first }}

---

<sub>Synced from `canonical/policies/ticketing-contract.md` in erpnext-ai-forge v{{ forge.version }} ({{ forge.source_commit | truncate(7, true, "") }}) at {{ forge.rendered_at }}. Shared by all seven tool adapters — do not hand-edit.</sub>
