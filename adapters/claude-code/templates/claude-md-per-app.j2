{#-
  Per-app CLAUDE.md (Decision 19).

  Two halves, deliberately:
    1. Facts this repo can derive — stack, counts, standard commands — rendered
       from discovery.
    2. Everything a human knows that no scanner can infer — rendered verbatim
       from `canonical/apps/<app>.md`.

  Half 2 used to be a per-app if/elif chain inside this template, which meant
  app knowledge lived in a rendering detail and only the apps someone had
  bothered to write a branch for got any. It is canonical content now: edit
  `canonical/apps/<app>.md`, not this file.

  Output: apps/<app>/CLAUDE.md
-#}
<!-- AUTO-GENERATED FROM erpnext-ai-forge {{ forge.version }} — DO NOT EDIT DIRECTLY -->
<!-- Edit `canonical/apps/{{ app.name }}.md` in erpnext-ai-forge, then re-run `forge sync`. -->
<!-- Hand edits are NOT silently lost: `forge sync` detects them and refuses to -->
<!-- overwrite; `forge adopt` folds them back into canonical for review. -->
# {{ app.name }} — App-Specific Context

**Stack:** {{ app.stack }}
**Custom DocTypes:** {{ app.doctype_count }}
**Whitelist APIs:** {{ app.whitelist_api_count }}
{% if app.get('purpose') %}**Purpose:** {{ app.get('purpose') }}{% endif %}

---

{% if app_notes %}{{ app_notes }}

---

{% else %}## Where Things Live

| Surface | Path |
|---------|------|
| Backend | `apps/{{ app.name }}/{{ app.name }}/` |
| API | `apps/{{ app.name }}/{{ app.name }}/api.py` (if present) |
| DocTypes | `apps/{{ app.name }}/{{ app.name }}/{{ app.name }}/doctype/` |
| Patches | `apps/{{ app.name }}/{{ app.name }}/patches/` |
| Fixtures | `apps/{{ app.name }}/{{ app.name }}/fixtures/` (if hook present) |

> No `canonical/apps/{{ app.name }}.md` yet — this app has only the derived
> facts above. Add that file in erpnext-ai-forge to give agents real context.

---

{% endif %}## Common Commands

```bash
source env/bin/activate
bench --site {{ bench.primary_site }} migrate          # after DocType/patch changes
bench --site {{ bench.primary_site }} clear-cache      # after hooks changes
bench --site {{ bench.primary_site }} export-fixtures --app {{ app.name }}   # capture Custom Fields / Workflows
```

---

<sub>Source: `canonical/apps/{{ app.name }}.md` in [erpnext-ai-forge](https://github.com/Kirito70/erpnext-ai-forge) v{{ forge.version }} ({{ forge.source_commit | truncate(7, true, "") }}). Synced at {{ forge.rendered_at }}.</sub>
