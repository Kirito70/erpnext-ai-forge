# erpnext-ai-forge .gitignore

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (if any tool template adds JS)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Secrets — NEVER commit
.env
.env.*
!.env.example
secrets/
*.local.yaml
*.local.yml
*.local.md
*.key
*.pem

# Audit log — local-only per v0.2 Decision 14
audit/**/*.jsonl
audit/**/*.jsonl.gz
audit/**/*.tar.gpg
!audit/.gitkeep

# Discovery — private (key names only is public; raw values are private)
discovery/data/*.private.json

# Staging dir used by `forge sync --all`
.forge-staging/

# Pre-merge snapshot of settings.json, taken fresh before every settings merge
# (see forge/src/forge/settings_merge.py). Recreated on every sync that
# touches settings.json, so tracking it would be permanent commit noise.
*.forge-backup

# Throwaway bench CI and `forge diff` render against. Today it holds only the
# ignored staging dir, so git happens not to see it — that stops being true the
# moment a sync writes a real file there.
.ci-fake-bench/

# Temporary
*.tmp
*.bak

# Generated reports (review before committing)
forge/reports/
